package model.objects.systems;

import java.awt.*;
import java.io.Serializable;

public class LinkSystem extends NetworkSystem implements Serializable {
    public LinkSystem(Point point) {
        super(point);
    }
}
